# NVENC SDK 直接出力プラグイン

このプラグインは NVENC SDK を直接使って GPU パスで H.264 / H.265 を出力します。

## 使い方
1. YMM4 で出力形式に「NVENC SDK 直接出力」が表示されます
2. GUIでコーデックとビットレートを設定して出力します
3. 出力形式は `.mp4` です

## 注意
- NVENC SDK / NVIDIA ドライバが必要です
